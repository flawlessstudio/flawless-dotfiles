# Unix-only projections. Loaded automatically because .miserc.toml enables auto_env.

[dotfiles]
"~/.zshrc" = { source = "dotfiles/zshrc", mode = "copy" }
"~/.tmux.conf" = { source = "dotfiles/tmux.conf", mode = "copy" }
